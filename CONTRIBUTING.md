# 기여 가이드 (Contributing)

`ai_development_structure` 프로젝트에 관심을 가져주셔서 감사합니다! 이 프로젝트는 AI와 개발자가 원활하게 협업할 수 있는 구조를 제안하는 오픈소스 템플릿입니다.

## 기여 방법

1. 이 저장소를 **Fork** 합니다.
2. 로컬에 저장소를 **Clone** 합니다.
3. 새로운 기능을 위한 **Branch**를 생성합니다. (`git checkout -b feat/add-new-template`)
4. 변경 사항을 **Commit** 합니다. (`git commit -m 'feat: 새로운 AI 프롬프트 템플릿 추가'`)
5. 원격 저장소에 **Push** 합니다. (`git push origin feat/add-new-template`)
6. 원본 저장소에 **Pull Request(PR)** 를 생성합니다.

## 기여 분야 (어떤 것을 기여할 수 있나요?)

* **템플릿 개선**: 더 나은 `.ai/prompts/` 양식 또는 시스템 지시문.
* **문서 오타 수정**: 기존 `docs/` 내의 설명 보강 및 오타/문맥 수정.
* **스크립트 고도화**: `init-local.ps1`, `init-project.js`, `init-project.sh` 등의 이식성 및 예외 처리 개선.
* **다양한 언어/IDE 지원**: Cursor, Windsurf 외의 새로운 AI IDE 연동 가이드 추가.

## 코딩 컨벤션

* 변경/추가되는 문서와 템플릿은 기본적으로 **한글(Korean)** 을 사용하여 작성합니다.
* 마크다운(Markdown) 포맷팅 가이드를 준수해 주세요.
* PR을 올릴 때는 미리 만들어둔 **PR 템플릿**에 맞추어 내용을 작성해 주시길 바랍니다.

감사합니다!
